<h1><%= @attraction.name %></h1>
    <% if current_user && current_user.admin %>
      <%= link_to "Edit Attraction", edit_attraction_path(@attraction) %>
    <% else %>
      <%= button_to "Go on this ride", update_user_path %>
    <% end %>
